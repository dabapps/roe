set -e

mkdir -p dist/js/
mkdir -p dist/css/

browserify -d src/ts/index.ts -p [tsify] | exorcist dist/js/index.js.map > dist/js/index.js
lessc src/less/index.less dist/css/index.css --silent --compress && postcss --use autoprefixer -r dist/css/index.css
